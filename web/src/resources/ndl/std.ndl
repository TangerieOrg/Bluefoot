// @ tags refer to the next item
// Nodes are implemented in js and only contain basic definitions

// @DisplayName Add
node add {
    inputs {
        Number a(1);
        Number b(2);
    }
    
    outputs {
        Number sum;
    }

    tags [
        Pure
    ]
}

//@DisplayName ToString (Number)
node numberToString {
    inputs {
        Execution execute;
        Number number;
        //@DisplayName Round?
        Boolean round;
    }

    outputs {
        Execution then;
        String string;
    }
}

node onKeyEvent {
    outputs {
        Execution pressed;
        Execution released;
        String key;
    }
    tags [ Event ]
}

// @DisplayName Length (String)
node stringLength {
    inputs {
        String string;
    }

    outputs {
        Number length;
    }

    tags [ Pure ]
}

node logString {
    inputs {
        Execution execute;
        String string;
    }

    outputs {
        Execution then;
    }

    tags [ Development ]
}

node ifElse {
	inputs {
		Execution execute;
		Boolean condition;
	}
	outputs {
		Execution if;
		Execution else;
	}
}

node while {
	inputs {
		Execution execute;
		Boolean condition;
	}
	outputs {
		Execution body;
		Execution end;
	}
}